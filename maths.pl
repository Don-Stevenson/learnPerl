#!/usr/bin/perl

use warnings;

 $length = 25;
  $width = 10;
  $area =
  $length *
  $width;


print "area is length $length multiplied by $width. The area is $area\n";

$radius = 5;
$PI = 3.14159256;
$circleArea = $PI * $radius * $radius;

print "Circle area is Pi($PI) R($radius)^2. The area is $circleArea\n";
